// Relative to the previous scroll settings. Locked sequences consume raw
// input, so Experience uses its own factor rather than multiplying both.
export const PAGE_SCROLL_SLOWDOWN = 2;
export const EXPERIENCE_SCROLL_SLOWDOWN = 4;
